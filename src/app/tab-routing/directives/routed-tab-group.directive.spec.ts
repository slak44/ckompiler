import { RoutedTabGroupDirective } from './routed-tab-group.directive';

describe('RoutedTabGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutedTabGroupDirective();
    expect(directive).toBeTruthy();
  });
});
