import { RoutedTabDirective } from './routed-tab.directive';

describe('RoutedTabDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutedTabDirective();
    expect(directive).toBeTruthy();
  });
});
