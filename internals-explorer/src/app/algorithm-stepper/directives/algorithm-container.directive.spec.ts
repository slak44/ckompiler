import { AlgorithmContainerDirective } from './algorithm-container.directive';

describe('AlgorithmContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new AlgorithmContainerDirective();
    expect(directive).toBeTruthy();
  });
});
